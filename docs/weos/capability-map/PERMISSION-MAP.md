# WEOS Permission Map

Runtime roles and canonical authority are not proven equivalent by the
repository. Treat the relationship as unresolved unless a future authority
document states otherwise.

| ID              | Runtime role           | Editorial permission                         | Guard/decorator                                            | Controller assertion                                                       | Available runtime actions                                                                                                | Restricted actions                                                               | Apparent canonical authority                    | Evidence                                                                                                                                  | Mismatch or uncertainty                                              |
| --------------- | ---------------------- | -------------------------------------------- | ---------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `WEOS-PERM-001` | `editor`               | `editor`                                     | `EditorialGuard`; `RequireEditorialPermission`             | `AdminGuard` checks metadata                                               | Access editorial workspace, generate drafts/candidates, request changes, edit drafts where endpoints allow               | Senior-only actions such as publish-like decisions, approvals in action registry | Not proven to be canonical decision authority   | `doctordle-backend/src/auth/roles.ts`; `auth/editorial.guard.ts`; `auth/editorial-permission.decorator.ts`; `admin.guard.ts`              | Runtime role grants access, not canonical authority                  |
| `WEOS-PERM-002` | `senior_editor`        | `senior`                                     | `SeniorEditorialGuard`; `RequireSeniorEditorialPermission` | `assertDraftLevelStatus` blocks senior-only statuses for non-publish roles | Approve/reject teaching rules, clue drafts, graph candidates; education review; publication readiness paths where routed | Admin-only operations if any separate admin guard path requires admin            | Not proven to equal publication authority       | `admin.controller.ts` `assertDraftLevelStatus`; `analytics-dashboard/src/features/editorial/workspace/actions/workspaceActionRegistry.ts` | Senior editor is runtime permission, not formal authority assignment |
| `WEOS-PERM-003` | `admin`                | editor-like and senior-like via role helpers | `AdminGuard`; `canAccessEditorial`; `canPublishEditorial`  | Admin bypasses some guard checks                                           | All guarded admin/editorial routes allowed by guard                                                                      | None at guard level identified                                                   | Not proven to be canonical governance authority | `auth/roles.ts`; `admin.guard.ts`; `roles.spec.ts`; `admin.guard.spec.ts`                                                                 | Admin operational access may exceed canonical editorial authority    |
| `WEOS-PERM-004` | Frontend action access | `editorial`, `seniorEditorial`               | Dashboard action registry only                             | None; backend still enforces guards                                        | Controls button visibility and disabled reasons                                                                          | Does not grant backend permission                                                | No canonical authority proof                    | `workspaceActionRegistry.ts`; `EditorialDiagnosisWorkspacePage.tsx` action access props                                                   | Frontend visibility must not be treated as authorization             |
| `WEOS-PERM-005` | Local QA role          | Env-driven role in auth guard                | `ClerkAuthGuard` local QA branch                           | N/A                                                                        | QA-only authenticated access depending env                                                                               | Production authority not implied                                                 | None                                            | `auth/clerk-auth.guard.ts`; `docs/editorial-local-qa-auth.md`                                                                             | QA convenience must not be interpreted as governance policy          |

## Explicit Authority Finding

The repository proves runtime access checks for `editor`, `senior_editor`, and
`admin`. It does not prove that those roles correspond to canonical decision
authorities such as publication authority, governance board authority, or
clinical/educational approval authority.
